{
  "name": "models",
  "version": "1.0.0-alpha.9",
  "license": "MIT",
  "repository": "Laboratoria/models",
  "scripts": {
    "pretest": "eslint .",
    "test": "jest --coverage --verbose --runInBand",
    "link-schemas": "rm -rf node_modules/schemas && ln -s ../../schemas ./node_modules/schemas",
    "changelog": "git log $(git describe --tags --abbrev=0)..HEAD --oneline --format=\"* %h %s (%an <%ae>)\""
  },
  "dependencies": {
    "jsdom": "^15.1.1",
    "mongoose-paginate": "^5.0.3",
    "schemas": "Laboratoria/schemas#v1.0.0-alpha.12",
    "semver": "^6.1.1"
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.18.2",
    "jest": "^24.8.0",
    "jest-environment-node": "^24.8.0",
    "mongodb": "^3.2.7",
    "mongodb-memory-server": "^5.1.9",
    "mongoose": "^5.6.7"
  },
  "peerDependencies": {
    "mongoose": "5.x"
  }
}
